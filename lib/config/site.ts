/** Canonical site URL for metadata (OpenGraph, canonical links, sitemap, JSON-LD).
 *  Override via NEXT_PUBLIC_SITE_URL if the platform is ever redeployed to a different domain. */
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.zidaproject.com";

export const SITE_NAME = "Zimbabwe Digital Investment & Economic Intelligence Platform";
